Ответьте на вопросы своими словами:

1. Что такое Enum?

    Enumeration означает перечисление. тоесть в энам можно писать константы связанные между собой по смыслу.
    такие как например дни недели, месяца, марки машин, языки. в энам используются только string.
    нельзя создавать объекты энам.

2. Приведите 3 примера где можно было бы использовать Enum?

    в задаче А использовала энам так как месяцы являются константами.
    при указании пола, женский или мужской.
    марки машин, телефонов, ноутбуков

3. Какие плюсы использования Enum вы знаете?



4. Когда лучше использовать LinkedList, ArrayList и обычный Array?

    ArrayList представляет собой обычный массив, но он намного удобнее, так как для него не указываеттся размер и можно добавлять новые элементы.
    ArrayList лучше использовать при set и get элементов так как он работает быстрее LinkedList.
    LinkedList используется при работе с очередями, и при вставлении элементов в середину так как это происходит быстрее чем в ArrayList.
    обычный массив лучше использовать когда знаешь определённый размер который не будет меняться.

5. Напишите кодом любой пример из 2. Пункта используя switch case

      public enum Cars {
           BMW,
           JEEP,
           FIAT;
       }

       public class Main {
           public static void main(String args[])
           {
               Cars c;
               c = Cars.AUDI;
               switch (c) {
               case BMW:
                   System.out.println("You choose BMW !");
                   break;
               case JEEP:
                   System.out.println("You choose JEEP !");
                   break;
               case FIAT:
                   System.out.println("You choose FIAT !");

               default:
                   System.out.println("NEW BRAND'S CAR.");
                   break;
               }
           }
       }